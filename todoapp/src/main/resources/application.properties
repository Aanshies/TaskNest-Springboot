# =========================
# PostgreSQL configuration
# =========================
spring.datasource.url=jdbc:postgresql://dpg-d20flcruibrs73860gr0-a:5432/tasknest_app_db
spring.datasource.username=tasknest_app_db_user
spring.datasource.password=75an7l2IeIgN6oXFUa1LZ38hmGDc1L9i

# ================
# JPA configuration
# ================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# ==========
# Thymeleaf
# ==========
spring.thymeleaf.cache=false
spring.thymeleaf.mode=HTML
logging.level.org.thymeleaf=DEBUG

# ==============
# Spring Security
# ==============
# Optional: You can remove these if using your own user registration flow
spring.security.user.name=admin
spring.security.user.password=admin

# ==========
# Server port
# ==========
# Allows Render to dynamically bind the provided PORT, fallback to 8080 locally
server.port=${PORT:8080}

# ================
# Hibernate dialect (optional but recommended for PostgreSQL)
# ================
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
